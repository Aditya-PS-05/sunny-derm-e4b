<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the generated PDF reports are ever shareable off-device, and only
         when the user explicitly taps Share. Photos/scans are never exposed. -->
    <files-path name="reports" path="reports/" />
</paths>
