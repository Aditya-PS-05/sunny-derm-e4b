<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only a decrypted, short-lived cache copy of a report is ever shareable
         off-device, and only when the user explicitly taps Share. The encrypted
         originals in files/reports and all photos are never exposed. -->
    <cache-path name="shared_reports" path="shared_reports/" />
</paths>
